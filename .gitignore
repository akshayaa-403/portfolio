# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# (js/config.js was ignored here against a js/config.example.js that never
#  existed, for a file no page loads. Rule removed rather than left to rot.)

# Editor directories and files
.vscode/*
!.vscode/extensions.json

# .claude/ holds the project guide (CLAUDE.md) and its reference docs, which
# belong in version control so they reach anyone else working on this. Only
# the machine-local settings and agent scratch are ignored.
.claude/settings.local.json
.claude/agents/
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Original artwork and photographs. The web-sized copies under
# public/assets/ are what ship; these are the masters and stay local.
sources/
